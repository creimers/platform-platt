div(layout="column")
  //user-role-cmp(on-select="$ctrl.setRole(role)")
  form(name="$ctrl.form")

    formly-form(
    form="$ctrl.form"
    model='$ctrl.model'
    fields='$ctrl.formFields'
    )

    google-autocomplete-cmp(
      on-select="$ctrl.setLocation(model)"
      placeholder="Wohnort *"
      )

  div(layout="row" layout-align="end center")
    md-progress-circular(
      md-mode="indeterminate"
      md-diameter="20"
      ng-show="$ctrl.isFetching"
      ).md-accent
    md-button.md-raised.md-accent(
      ng-click="$ctrl.onRegister()"
      ng-disabled="$ctrl.form.$invalid || $ctrl.isFetching || !$ctrl.model.location"
      ) registrieren
